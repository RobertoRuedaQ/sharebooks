
     <div class="col-md-3">
      <p class="lead">Categorías</p>
     <div class="list-group">
      <% @categories.each do |category| %>
        <li><%= link_to "#{category.name}", params: {category:category.id}%></li> 
      <%end %>
     </div>
    </div>